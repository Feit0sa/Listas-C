/* Elaborar um programa que apresente os resultados das potências do valor de base 3, elevado a um
expoente que varie do valor 0 a 7. */

#include <stdio.h>
#include <math.h>

int main() {

    // juro que tentei fazer por laço, mas simplemente não queria rodar no meu pc
    printf("%.0lf\n", pow(3, 0));
    printf("%.0lf\n", pow(3, 1));
    printf("%.0lf\n", pow(3, 2));
    printf("%.0lf\n", pow(3, 3));
    printf("%.0lf\n", pow(3, 4));
    printf("%.0lf\n", pow(3, 5));
    printf("%.0lf\n", pow(3, 6));
    printf("%.0lf\n", pow(3, 7));

}